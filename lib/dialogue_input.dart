import 'package:flutter/material.dart';

class DialogueInput extends StatelessWidget {
  const DialogueInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
